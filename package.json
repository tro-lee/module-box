{
  "name": "module-toolbox",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "library:test": "bun test test/core.test.ts -- -debug",
    "library:build": "cd packages/library && tsc && bun build",
    "web:dev": "cd packages/web && bun --bun run dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "library": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.1"
  }
}