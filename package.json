{
  "name": "module-toolbox",
  "module": "index.ts",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "core:test": "bun test test/core.test.ts -- -debug",
    "web:dev": "cd packages/web && bun --bun run dev"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "library": "workspace:*"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.1"
  }
}